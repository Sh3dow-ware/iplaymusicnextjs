'use client'

import { ThemeProvider } from "@/components/providers/ThemeProvider";

export default function ArtistPage() {
  return (
    <ThemeProvider>
      <div>Hello Artist Page!</div>
    </ThemeProvider>
  )
}
